# cluster specific values
cluster:
  # specify cluster name
  name: aws-cluster

  # specify cluster labels
  labels: {}

  # specify cluster annotations
  annotations: {}

# specify cloud credential secret name, do not need to be provided if using custom driver
cloudCredentialSecretName: "cattle-global-data:cc-bzv2q"
region: "eu-west-1"
vpcId: "vpc-0ec73487217ffcc91"
